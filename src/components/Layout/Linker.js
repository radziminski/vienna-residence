import React from 'react';

const Linker = (props) => {
    return (
        <div className="linker" style={{backgroundColor: props.color}}>
            &nbsp;
        </div>
    )
}

export default Linker;
